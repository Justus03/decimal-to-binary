# ifndef _M_HEADERS_H_INCLUDED
# define _M_HEADEARS_H_INCLUDED

int division_entiere(int *, int, int *, int*, int*);
void complement_2(int*, int);
void afficheur_tableau(int*, int, int, char);// int:*: pointeur sur un tableau, int: taille max du tableau, int: numéros de la case qui sera prise comme reférence pour débuter la lecture, char: correspond au mode de lecture( entrez 'h' pour lire du debut de la case de reférence vers le début du tableau et entrez 't' pour lire du début de la case de référence vers la fin du tableau).
void choice(int*);

# endif
